// ===========================================
// ENUMS
// ===========================================

export enum UserRole {
  ADMIN = "admin",
  STAFF = "staff",
}

export enum ArticleStatus {
  DRAFT = "draft",
  PUBLISHED = "published",
  ARCHIVED = "archived",
}

export enum PageType {
  HOME = "home",
  CATEGORY = "category",
  ARTICLE = "article",
}

// ===========================================
// Types akan ditambahkan nanti seiring development
// ===========================================
